apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ai-platform.fullname" . }}-test-connection"
  labels:
    {{- include "ai-platform.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ai-platform.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
